import React, {useState} from 'react';
import styled from "./Update.scss";
const Update = () => {
    return (
        <div>
            <input type="text"/>

            
        </div>
    );
};

export default Update;
